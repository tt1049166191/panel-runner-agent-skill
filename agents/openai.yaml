interface:
  display_name: "Panel Runner"
  short_description: "独立运行项目、监听 JSON，并在终态安全静态化面板"
  default_prompt: "Use $panel-runner to run this project independently with a live JSON panel and safe terminal cleanup."
policy:
  allow_implicit_invocation: true
